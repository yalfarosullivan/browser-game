const object = document.getElementById("example");
const slized = new Slizer(object);

const slized = new Slizer(object, {
    Direction : 'v'
});

const slized = new Slizer(object, 
    RoundInterval : 100,
    PixelPerRound : 1
);

const slized = new Slizer(object, repeat);